#include"stdio.h"
void find_max(long int x,long int y)
{
	if ( a > b )
	{	printf ("a is greater\n");
	}
	else
	{	printf ("b is greater\n");
}
}
int main()
{
	long int A[2];
	A[1] = 2; 
	A[0] = 3;
	find_max(A[1],A[0]);
	return 0;	
}

/* Expected output: This code will not show errors and it should display the data types, keywords, identifiers, functions and operators as tokens. */